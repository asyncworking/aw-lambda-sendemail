exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: getRandomInt(event.max),
    };
    return response;
};

function getRandomInt(max = 20) {

  return Math.floor(Math.random() * Math.floor(max));

}

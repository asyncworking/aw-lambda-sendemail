{
  "name": "sending_email",
  "version": "1.0.0",
  "description": "lambda function for sending verification email",
  "main": "sending_email.js",
  "scripts": {
    "test": "jest --config=jest.config.js --coverage --color --watchAll=false"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.797.0",
    "aws-sdk-mock": "^5.2.1",
    "dotenv": "^10.0.0",
    "jest": "^26.4.2",
    "jest-cli": "^27.0.6",
    "lambda-tester": "^4.0.1"
  }
}
